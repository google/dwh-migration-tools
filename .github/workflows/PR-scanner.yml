name: PR Focused Dependency Scan

on:
  pull_request:
    paths:
      - '**/gradle.lockfile'

jobs:
  osv-scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR Branch
        uses: actions/checkout@v4
        with:
          path: pr

      - name: Checkout Main Branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.base_ref }}
          path: main

      - name: Install OSV-Scanner
        run: go install github.com/google/osv-scanner/cmd/osv-scanner@latest

      - name: Process Lockfile Diff and Scan New Deps
        run: |
          # 1. Extract Blessed CVE IDs from your YAML [cite: 101, 102]
          BLESSED_IDS=$(yq eval '.[].CVE' pr/.security/known_cves.yml | paste -sd "|" -)

          # 2. Process Lockfile Diff 
          # Find all unique lines added in PR that weren't in Main
          find pr -name "gradle.lockfile" -exec grep -hve "^#" -ve "^$" {} + | sort > pr_deps.txt
          find main -name "gradle.lockfile" -exec grep -hve "^#" -ve "^$" {} + | sort > main_deps.txt
          
          # Isolate only new or changed package lines (e.g., snowflake-jdbc:3.21.0)
          comm -13 main_deps.txt pr_deps.txt > delta_deps.txt

          if [ ! -s delta_deps.txt ]; then
            echo "No new dependencies found in lockfiles. Skipping scan."
            exit 0
          fi

          # 3. Google OSV Scan [cite: 60, 61]
          # Flags must come BEFORE the path [cite: 92, 93]
          ~/go/bin/osv-scanner scan --format json -r pr > pr_results.json || true

          # 4. Filter Logic
          # Only fail if a vulnerability ID matches a CHANGED package and is NOT BLESSED
          NEW_UNHANDLED_VULNS=""
          while read -r dep; do
            # Extract package name (e.g., net.snowflake:snowflake-jdbc)
            pkg_name=$(echo "$dep" | cut -d':' -f1,2)
            
            # Match package name against scan results and filter out blessed IDs [cite: 44]
            found=$(jq -r --arg PKG "$pkg_name" '
              .results[].vulnerabilities[]? | 
              select(.package.name == $PKG) | .id' pr_results.json | \
              grep -Ev "$BLESSED_IDS" || true)
            
            if [ -n "$found" ]; then
              NEW_UNHANDLED_VULNS="$NEW_UNHANDLED_VULNS $found"
            fi
          done < delta_deps.txt

          if [ -n "$NEW_UNHANDLED_VULNS" ]; then
            echo "Build FAIL: New vulnerabilities detected in changed dependencies: $NEW_UNHANDLED_VULNS"
            exit 1
          else
            echo "Build PASS: Changes are secure or blessed."
          fi
